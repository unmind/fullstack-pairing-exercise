export const reportingService = {
  reportNewCheckin: async (params: any) => params,
};

export const notificationService = {
  notify: async (params: any) => params,
};

export const aggregatorService = {
  updateStats: async (params: any) => params,
};
